/**
 * 
 */
var x=1; //initialization
while (x<5) //condition
	{
	console.log(x);
	x++; //condition to exit the loop
	}
//In loader example, one can use while loop

/* var z=2; // THIS LLOOP DOESN'T WORK WELL.
do {
	console.log(z);
	
} while(z++)     //Run at least once
*/

var z=10;
do {
	console.log(z);
	z++;
	//console.log(z);
	//z++;
} while(z<12)